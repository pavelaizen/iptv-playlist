services:
  playlist-static:
    image: nginx:alpine
    container_name: playlist-static
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./published:/usr/share/nginx/html:ro
