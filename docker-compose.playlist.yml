services:
  playlist-static:
    image: nginx:alpine
    container_name: playlist-static
    restart: unless-stopped
    ports:
      - "8766:80"
    volumes:
      - ./published:/usr/share/nginx/html:ro
      - ./nginx/playlist-static.conf:/etc/nginx/conf.d/default.conf:ro
